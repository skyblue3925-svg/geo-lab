streamlit>=1.28.0
numpy>=1.24.0
plotly>=5.18.0
matplotlib>=3.7.0
scipy>=1.11.0
Pillow>=10.0.0
kaleido>=0.2.1
supabase>=2.0.0
pytest>=8.0.0

# Optional PyVista/stpyvista stack is intentionally excluded from the beta deploy.
# The beta Animation Studio uses a browser-native Three.js viewer instead.
