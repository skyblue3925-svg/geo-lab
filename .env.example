# Copy this file to `.env` and fill in your Telegram bot settings.
# The daily brief publisher reads these values in this order:
# 1. OS env vars
# 2. repo-local `.env`
# 3. shared `C:\Users\HANSOL\.codex\telegram-daily-brief.env`
# For multiple vaults, prefer the shared file and only use repo-local `.env` for overrides.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
